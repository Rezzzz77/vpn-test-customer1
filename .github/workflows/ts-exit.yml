name: ts-exit

# خروجی آمریکا مستقیم روی رانر Actions با Tailscale (بدون هاپ Cloudflare)
# پیش‌نیاز: سکرت TS_AUTHKEY (کلید احراز Tailscale از نوع Ephemeral + Reusable)
# گوشی: اپ Tailscale/Aethery → لاگین همان اکانت → Use exit node → انتخاب این نود
# زنجیرهٔ خودگردان: هر جاب قبل از پایان سقف ۳۵۰ دقیقه‌ای، جانشین بعدی را dispatch می‌کند

on:
  workflow_dispatch:
    inputs:
      hostname:
        description: 'نام نود در Tailscale (خالی = خودکار)'
        required: false
        default: ''

concurrency:
  group: ts-exit-node
  cancel-in-progress: true

permissions:
  contents: write
  actions: write

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 350   # سقف جاب ۶ ساعت است؛ ۳۵۰ دقیقه حاشیه امن
    steps:
      - uses: actions/checkout@v4

      - name: آماده‌سازی TUN و IP forwarding
        run: |
          sudo modprobe tun 2>/dev/null || true
          if [ ! -c /dev/net/tun ]; then
            sudo mkdir -p /dev/net
            sudo mknod /dev/net/tun c 10 200
          fi
          ls -l /dev/net/tun
          sudo sysctl -w net.ipv4.ip_forward=1
          sudo sysctl -w net.ipv6.conf.all.forwarding=1

      - name: نصب Tailscale
        run: |
          curl -fsSL https://tailscale.com/install.sh | sh
          tailscale version

      - name: اجرای tailscaled و اتصال به عنوان Exit Node
        run: |
          sudo mkdir -p /var/run/tailscale /var/lib/tailscale
          sudo setsid nohup tailscaled \
            --state=/var/lib/tailscale/ts.state \
            --statedir=/var/lib/tailscale \
            --socket=/var/run/tailscale/tailscaled.sock \
            > /tmp/tscaled.log 2>&1 &
          sleep 3
          HN="${{ github.event.inputs.hostname }}"
          [ -z "$HN" ] && HN="gh-exit"
          sudo tailscale --socket=/var/run/tailscale/tailscaled.sock up \
            --authkey="${{ secrets.TS_AUTHKEY }}" \
            --hostname="$HN" \
            --advertise-exit-node \
            --accept-dns=false \
            --accept-risk=lose-ssh
          echo "### وضعیت نود"
          sudo tailscale --socket=/var/run/tailscale/tailscaled.sock status
          echo "TS_IP=$(sudo tailscale --socket=/var/run/tailscale/tailscaled.sock ip -4)" >> "$GITHUB_ENV"
          # تشخیص مسیر: مستقیم یا relay (برای مانیتورینگ پینگ/سرعت)
          sudo tailscale --socket=/var/run/tailscale/tailscaled.sock status --json > /tmp/tsstatus.json 2>/dev/null || true
          PYCODE='import json,time; d=json.load(open("/tmp/tsstatus.json")); peers=d.get("Peer",{}) or {}; me=d.get("Self",{}); out={"time":time.strftime("%FT%TZ",time.gmtime()),"self":{"online":me.get("Online"),"dnsName":(me.get("DNSName") or "").rstrip(".")},"peers":[{"name":(p.get("DNSName") or "").rstrip("."),"online":p.get("Online"),"relay":p.get("Relay"),"curAddr":p.get("CurAddr"),"addrs":p.get("Addrs") or [],"rxBytes":p.get("RxBytes"),"txBytes":p.get("TxBytes")} for k,p in peers.items()]}; print(json.dumps(out))'
          python3 -c "$PYCODE" > ts_peer_status.txt 2>/dev/null || true
      - name: گزارش نود (IP عمومی رانر + IP تیل‌نت)
        run: |
          PUB=$(curl -s --max-time 10 https://api.ipify.org || echo '?')
          echo "🖥  Public IP رانر : $PUB"
          echo "🔗  TS IP نود      : $TS_IP"
          echo "$PUB" > ts_public_ip.txt
          git config user.name  "ts-bot"
          git config user.email "ts-bot@users.noreply.github.com"
          git add ts_public_ip.txt ts_peer_status.txt 2>/dev/null || git add ts_public_ip.txt
          git commit -m "ts-exit: runner $PUB @ $(date -u +%FT%TZ)" || echo "nothing to commit"
          git push || echo "(push failed - فقط گزارش)"

      - name: نگهبانی و زنده نگه داشتن + زنجیرهٔ خودگردان
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TS="sudo tailscale --socket=/var/run/tailscale/tailscaled.sock"
          END=$((SECONDS + 20200))
          FAILS=0
          while [ $SECONDS -lt $END ]; do
            sleep 60
            # سلامت اتصال عمومی
            CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 12 https://www.google.com/generate_204 || echo 000)
            # سلامت tailscaled
            if ! $TS status >/dev/null 2>&1; then
              FAILS=$((FAILS+1))
              echo "[$(date -u +%T)] tailscaled پاسخ نمی‌دهد ($FAILS) — تلاش مجدد up"
              sudo tailscaled --state=/var/lib/tailscale/ts.state --statedir=/var/lib/tailscale \
                --socket=/var/run/tailscale/tailscaled.sock >> /tmp/tscaled.log 2>&1 &
              sleep 5
              $TS up --authkey="${{ secrets.TS_AUTHKEY }}" --hostname="gh-exit" \
                --advertise-exit-node --accept-dns=false --accept-risk=lose-ssh 2>/dev/null || true
            else
              FAILS=0
            fi
            echo "[$(date -u +%T)] google=$CODE alive"
            if [ "$FAILS" -ge 10 ]; then
              echo "tailscaled مرده — dispatch نود جانشین قبل از خروج"
              gh workflow run ts-exit.yml --repo Rezzzz77/gh-exit -f hostname=gh-exit 2>/dev/null || true
              exit 1
            fi
          done
          # پایان دور نگهبانی — قبل از کشته‌شدن جاب، جانشین را dispatch کن
          echo "⏳ پایان دور نگهبانی — dispatch نود بعدی..."
          # ضدچرخش هوشمند: مقایسه با «زمان باقیمانده» — اگر نود دیگری زنده است و
          # بیش از ۱۰ دقیقه (۶۰۰ ثانیه) عمر جلو دارد، dispatch نکن تا concurrency آن را نکشد.
          # (self با GITHUB_RUN_ID مستثنی می‌شود؛ فقط ران‌های in_progress/queued دیگر شمرده می‌شوند)
          RUNS_JSON=$(curl -s --max-time 20 -H "Authorization: Bearer $GH_TOKEN" \
            "https://api.github.com/repos/Rezzzz77/gh-exit/actions/workflows/ts-exit.yml/runs?per_page=8" || true)
          ALIVE=$(printf '%s' "$RUNS_JSON" | ME="${GITHUB_RUN_ID:-0}" python3 -c 'import json,sys,os,datetime; d=json.loads(sys.stdin.read() or "{}"); me=int(os.environ.get("ME","0") or 0); now=datetime.datetime.now(datetime.timezone.utc); rem=[int(20200-(now-datetime.datetime.fromisoformat(r["created_at"].replace("Z","+00:00"))).total_seconds()) for r in d.get("workflow_runs",[]) if r.get("id")!=me and r.get("status") in ("in_progress","queued")]; print(max(rem) if rem else 0)')
          echo "   بیشترین عمر باقی‌ماندهٔ نود دیگر: ${ALIVE:-0} ثانیه"
          if [ "${ALIVE:-0}" -gt 600 ] 2>/dev/null; then
            echo "⏭️ نود تازه‌ای با بیش از ۱۰ دقیقه عمر زنده است — dispatch لغو شد (جلوگیری از کشتن نود سالم)"
          else
            for i in 1 2 3; do
              if gh workflow run ts-exit.yml --repo Rezzzz77/gh-exit -f hostname=gh-exit; then
                echo "✅ نود جانشین dispatch شد — زنجیره ادامه دارد"
                break
              fi
              echo "dispatch ناموفق (تلاش $i)"; sleep 10
            done
          fi
